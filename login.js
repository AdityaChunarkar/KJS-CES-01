document.getElementById("loginForm").addEventListener("submit",e=>{e.preventDefault();const email=loginEmail.value.trim(),password=loginPassword.value,u=getUser();if(u&&u.email===email&&u.password===password){localStorage.setItem(LOGIN_KEY,"true");location.href="dashboard.html"}else{loginError.textContent=u?"Invalid email or password.":"Create an account first.";loginError.hidden=false}});